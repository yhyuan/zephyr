description: |
    This binding gives a base representation of the GD32 USART

compatible: "gigadevice,gd32-usart"

include: uart-controller.yaml

properties:
    reg:
      required: true

    interrupts:
      required: true
